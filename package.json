{
  "name": "boomin-sdk",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/cli",
    "packages/connect",
    "packages/server"
  ],
  "scripts": {
    "build": "npm run build --workspace @boomin/connect && npm run build --workspace @boomin/server && npm run build --workspace @boomin/cli",
    "pack:dry": "npm pack --dry-run --workspace @boomin/connect && npm pack --dry-run --workspace @boomin/server && npm pack --dry-run --workspace @boomin/cli"
  }
}
