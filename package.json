{
  "name": "boomin-sdk",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/cli",
    "packages/connect",
    "packages/sdk",
    "packages/server"
  ],
  "scripts": {
    "build": "npm run build --workspace @boomin/connect && npm run build --workspace @boomin/server && npm run build --workspace @boomin/cli && npm run build --workspace @boomin/sdk",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "pack:dry": "npm pack --dry-run --workspace @boomin/connect && npm pack --dry-run --workspace @boomin/server && npm pack --dry-run --workspace @boomin/cli && npm pack --dry-run --workspace @boomin/sdk"
  }
}
